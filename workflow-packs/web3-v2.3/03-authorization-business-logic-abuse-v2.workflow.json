{
  "kind": "open-kritt-workflow",
  "version": 2,
  "workflow": {
    "name": "Authorization and Business Logic Abuse — Progressive v2.3",
    "description": "Scope-aware actor-resource-action mapping, independent authorization and workflow abuse fan-out, root-cause deduplication, adversarial verification, stateful PoC, eligibility gating, and ranking. Import-valid v2.3: globally unique stage output keys, one consume-all dedup boundary, immutable program extras after dedup, deterministic mapping, high-priority PoC gating, and explicit Web3 fork inputs. Progressive v2.3 restores the original fan-out behavior: each independently checked candidate reaches the final finding depth while sibling investigations continue. Internal global deduplication was removed because OpenKritt already deduplicates and ranks final findings during post-processing.",
    "levels": [
      {
        "depth": 0,
        "multiOutput": false,
        "consumesAll": false,
        "bindPrevious": false,
        "outputFormat": {
          "scope_scope_contract_id": "string",
          "scope_in_scope_assets": "array",
          "scope_accepted_impacts": "array",
          "scope_excluded_issues": "array",
          "scope_known_issues": "array",
          "scope_program_rules": "array",
          "scope_payout_priorities": "array",
          "scope_deployment_facts": "array",
          "scope_authorized_test_constraints": "array",
          "scope_scope_gaps": "array",
          "scope_scope_evidence": "array"
        },
        "steps": [
          {
            "name": "Freeze program scope contract",
            "content": "Repository: {{repo_full}} at commit {{commit_sha}}.\nProgram URL: {{extra.bug_bounty_url}}\nRules checked at: {{extra.program_rules_checked_at}}\nProgram snapshot: {{extra.program_rules_snapshot}}\nDeployment evidence: {{extra.deployment_evidence}}\nKnown-issues snapshot: {{extra.known_issues_snapshot}}\nAuthorized testing constraints: {{extra.authorized_test_constraints}}\n\nBuild the immutable scope contract for this run before analyzing code. Extract exact in-scope assets/components, accepted impacts, exclusions, known issues, testing restrictions, payout priorities, and supplied deployment facts. Separate supplied evidence from missing facts. Never infer current program rules, live deployments, TVL, traffic, configuration, or severity from memory. A silent snapshot creates a scope gap.\n\nReturn exactly one record. Preserve the wording or a close identifier for each controlling rule so later agents can map a technical effect to a named accepted impact. Do not analyze vulnerabilities."
          }
        ],
        "isLastStep": false
      },
      {
        "depth": 1,
        "multiOutput": true,
        "consumesAll": false,
        "bindPrevious": false,
        "outputFormat": {
          "surface_boundary_id": "string",
          "surface_boundary_label": "string",
          "surface_entrypoint": "string",
          "surface_entrypoint_file_path": "string",
          "surface_entrypoint_line": "number",
          "surface_least_privileged_actor": "string",
          "surface_subject_identity_source": "string",
          "surface_tenant_identity_source": "string",
          "surface_resource_identity_source": "string",
          "surface_resource_or_state": "string",
          "surface_controlled_identifiers_and_fields": "array",
          "surface_intended_authorization_invariant": "string",
          "surface_intended_state_invariant": "string",
          "surface_authentication_trace": "array",
          "surface_authorization_trace": "array",
          "surface_workflow_state_trace": "array",
          "surface_terminal_effect": "string",
          "surface_candidate_allowed_impacts": "array",
          "surface_reachability_evidence": "array",
          "surface_evidence_gaps": "array"
        },
        "steps": [
          {
            "name": "Map actor-resource-action and workflow boundaries",
            "content": "Map every in-scope production operation where an external actor reads protected data, mutates state, spends value/quota, changes identity/privilege, crosses a tenant, invokes administration, or advances a sensitive workflow. Prove registration. For each distinct actor-resource-action boundary, trace how subject, tenant, object, role, and workflow state are derived through middleware, policy helpers, services, queues, and downstream calls. Record the least-privileged actor, controlled IDs/fields, intended authorization and state invariants, enforcement traces, terminal effect, and matching accepted-impact candidates. No vulnerabilities.\n\nUse deterministic enumeration before reasoning: ripgrep plus language-aware AST/compiler tooling when available. Perform {{extra.repeat_runs}} independent enumeration passes when supplied (default: 2), compare their coverage, and record any coverage gap."
          }
        ],
        "isLastStep": false
      },
      {
        "depth": 2,
        "multiOutput": true,
        "consumesAll": false,
        "bindPrevious": false,
        "outputFormat": {
          "hyp_hypothesis_id": "string",
          "hyp_hypothesis_class": "string",
          "hyp_hypothesis_summary": "string",
          "hyp_root_cause": "string",
          "hyp_root_cause_location": "string",
          "hyp_broken_invariant": "string",
          "hyp_attacker_sequence": "array",
          "hyp_required_preconditions": "array",
          "hyp_expected_technical_impact": "string",
          "hyp_matched_allowed_impact_candidate": "string",
          "hyp_candidate_guards": "array",
          "hyp_verification_locations": "array",
          "hyp_falsification_target": "string",
          "hyp_evidence_gaps": "array"
        },
        "steps": [
          {
            "name": "Hypotheses A: identity, object, tenant, privilege",
            "content": "Fresh-context analysis of boundary {{surface_boundary_label}} at {{surface_entrypoint_file_path}}:{{surface_entrypoint_line}}. Generate one hypothesis per precise horizontal/vertical privilege change, cross-tenant substitution, IDOR/BOLA, confused deputy, mass assignment, subject/object mismatch, alternate endpoint, stale authorization, role escalation, canonicalization collision, or downstream authorization gap. Each record must name the changed attacker field/sequence, broken invariant, exact suspected guard gap, locations, preconditions, accepted impact, candidate guards, and strongest falsification target."
          },
          {
            "name": "Hypotheses B: state machine and multi-request abuse",
            "content": "Independently analyze boundary {{surface_boundary_label}} with fresh context. Generate one hypothesis per precise step skip, replay, duplicate, reordering, race, TOCTOU, partial failure, retry/idempotency error, negative/boundary value, quota/accounting drift, cancellation/rollback, or cross-channel workflow abuse. Require a concrete attacker sequence, reachable state prerequisites, broken invariant, faulty location, terminal security effect, accepted-impact candidate, and falsification target. No generic checklist."
          }
        ],
        "isLastStep": false
      },
      {
        "depth": 3,
        "multiOutput": false,
        "consumesAll": false,
        "bindPrevious": false,
        "outputFormat": {
          "verify_technical_verdict": "string",
          "verify_production_reachability": "string",
          "verify_verified_trace": "array",
          "verify_attacker_control_evidence": "array",
          "verify_guard_analysis": "array",
          "verify_falsification_attempts": "array",
          "verify_runtime_evidence": "array",
          "verify_unresolved_assumptions": "array",
          "verify_observed_effect": "string",
          "verify_verified_file_path": "string",
          "verify_verified_line": "number",
          "verify_poc_plan": "array",
          "poc_poc_verdict": "string",
          "poc_poc_method": "string",
          "poc_commands_or_test_cases": "array",
          "poc_poc_evidence": "array",
          "poc_observed_effect": "string",
          "poc_expected_vs_observed": "string",
          "poc_repeatability": "string",
          "poc_artifact_paths": "array",
          "poc_minimal_reproduction": "array",
          "poc_remaining_assumptions": "array"
        },
        "steps": [
          {
            "name": "Independently verify and reproduce candidate",
            "content": "Independently verify one code-grounded hypothesis without trusting the upstream conclusion.\n\nCandidate id: {{hyp_hypothesis_id}}\nSummary: {{hyp_hypothesis_summary}}\nRoot cause: {{hyp_root_cause}} at {{hyp_root_cause_location}}\nAttacker sequence: {{hyp_attacker_sequence}}\nBroken invariant: {{hyp_broken_invariant}}\nCandidate guards: {{hyp_candidate_guards}}\nExpected impact: {{hyp_expected_technical_impact}}\nRequired preconditions: {{hyp_required_preconditions}}\nEvidence gaps: {{hyp_evidence_gaps}}\nStrongest falsification target: {{hyp_falsification_target}}\n\nFirst perform hostile technical verification. Trace the actual production code and try to disprove attacker control, reachability, the missing guard, and the claimed terminal effect. Do not carry a candidate forward merely because it is plausible.\n\nAs a new verifier, try to disprove {{hyp_hypothesis_id}}. Reconstruct authentication -> subject/tenant derivation -> object loading -> authorization -> state transition -> terminal effect across middleware, services, jobs, and downstream checks. Prove control over each identifier, field, timing, and prerequisite state. Inspect ownership, tenancy, policy caching, idempotency, database constraints, isolation/locking, transactions, rollback, feature flags, and alternate guards. supported requires a complete production path and concrete cross-boundary or security-sensitive effect; otherwise falsified/blocked. Produce a minimal isolated multi-account reproduction plan.\n\nIf and only if the technical verdict remains supported, immediately attempt the smallest safe deterministic reproduction in this same job. A failed or blocked reproduction must be recorded honestly; it must not erase a technically supported candidate that still needs manual validation.\n\nFor supported candidates, reproduce in an authorized isolated environment with the smallest stateful sequence and at least two principals/tenants when relevant. Capture identities/roles, object ownership, requests/events, before/after state, response and downstream effect. Prefer an integration/regression test and repeat the decisive sequence. Respect {{extra.authorized_test_constraints}}. Set PoC verdict and preserve commands, evidence, artifacts, minimal reproduction, and assumptions.\n\nFor smart-contract candidates, use {{extra.fork_rpc_url}} and {{extra.fork_block}} when supplied. Never send transactions to a public network.\n\nDo not apply the bounty-program eligibility decision and do not write the final finding in this step. Populate every verify_* and poc_* field. Use technical_verdict=falsified when the code contradicts the hypothesis, blocked only for a specific missing fact or unavailable environment, and supported only for a complete code-grounded path."
          }
        ]
      },
      {
        "depth": 4,
        "multiOutput": true,
        "consumesAll": false,
        "bindPrevious": false,
        "outputFormat": {
          "explanation": "string",
          "file_path": "string",
          "line": "number",
          "malicious_input_example": "string",
          "summary": "string",
          "trigger_flow": "array",
          "vulnerability_type": "string",
          "malicious_actor": "string",
          "exploitable": "boolean",
          "root_bug_fingerprint": "string",
          "technical_verdict": "string",
          "poc_verdict": "string",
          "eligibility_verdict": "string",
          "program_asset": "string",
          "program_impact": "string",
          "severity_candidate": "string",
          "report_confidence": "string",
          "remaining_assumptions": "array",
          "evidence_artifacts": "array"
        },
        "steps": [
          {
            "name": "Apply program gate and emit progressive finding",
            "content": "Apply the immutable bounty-program evidence to one independently checked candidate, then emit its progressive finding record.\n\nProgram URL: {{extra.bug_bounty_url}}\nRules checked at: {{extra.program_rules_checked_at}}\nProgram snapshot: {{extra.program_rules_snapshot}}\nDeployment evidence: {{extra.deployment_evidence}}\nKnown issues: {{extra.known_issues_snapshot}}\nAuthorized testing constraints: {{extra.authorized_test_constraints}}\n\nCandidate id: {{hyp_hypothesis_id}}\nSummary: {{hyp_hypothesis_summary}}\nRoot cause: {{hyp_root_cause}} at {{hyp_root_cause_location}}\nAttacker sequence: {{hyp_attacker_sequence}}\nExpected impact: {{hyp_expected_technical_impact}}\nCandidate accepted impact: {{hyp_matched_allowed_impact_candidate}}\n\nTechnical verdict: {{verify_technical_verdict}}\nProduction reachability: {{verify_production_reachability}}\nObserved technical effect: {{verify_observed_effect}}\nVerified trace: {{verify_verified_trace}}\nGuard analysis: {{verify_guard_analysis}}\nTechnical assumptions: {{verify_unresolved_assumptions}}\n\nPoC verdict: {{poc_poc_verdict}}\nPoC evidence: {{poc_poc_evidence}}\nPoC observed effect: {{poc_observed_effect}}\nPoC artifacts: {{poc_artifact_paths}}\nPoC assumptions: {{poc_remaining_assumptions}}\n\nReturn no records when the hypothesis was falsified, is a known duplicate, requires a privileged or prohibited actor, affects an out-of-scope asset, matches no accepted impact, or remains speculative without a concrete faulty operation and attacker-to-impact trace.\n\nReturn one record when either:\n1. the candidate is technically supported, eligible, and reproduced or deterministically proven; set exploitable=true and report_confidence=verified; or\n2. the candidate is technically supported with strong code evidence but the safe local reproduction or one deployment/program fact is specifically blocked; set exploitable=false, eligibility_verdict=blocked when applicable, and report_confidence=needs_manual_validation.\n\nThe second case is a visible research candidate, not a bounty-ready claim. State the exact blocker in remaining_assumptions. Never upgrade blocked evidence to a verified finding.\n\nCreate a stable root_bug_fingerprint from the faulty operation, attacker primitive, broken invariant, and terminal effect so the engine's built-in post-processing can deduplicate and rank findings globally. Point file_path and line to the actual faulty operation or missing protection. Keep trigger_flow ordered from attacker input to impact. Use the smallest safe malicious_input_example suitable for an authorized local environment."
          }
        ]
      }
    ]
  }
}
